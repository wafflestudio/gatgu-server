version: "3.9"
services:
    web:
      build: 
          context: ./
          dockerfile : Dockerfile
      command: gunicorn gatgu.wsgi:application --bind 0.0.0.0:8000
      ports:
        - "8000:8000"
    redis:
      image: redis:latest
